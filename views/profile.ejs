<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>Document</title>
  </head>

  <body>
    <div class="w-full p-10">
      <a
        href="/logout"
        class="btn btn-danger"
        tabindex="-1"
        role="button"
        aria-disabled="true"
        >Logout</a
      >
      <h3>Hello, <%= user.name %></h3>
      <form action="/post" method="post">
        <div class="mb-3">
          <label for="exampleFormControlTextarea1" class="form-label"
            >You can create a new post</label
          >
          <textarea
            placeholder="What's on your mind ?"
            class="mb-3 form-control"
            name="content"
            id="exampleFormControlTextarea1"
            rows="3"
          ></textarea>
          <input
            type="submit"
            class="btn btn-primary btn-lg"
            value="create a post"
          />
        </div>
      </form>
      <div class="posts" style="margin-top: 50px">
        <h5 style="color: gray">Your posts.</h5>
        <% user.post.reverse().forEach((post)=> { %>
        <div class="card" style="width: 33%; margin-top: 20px">
          <div class="card-body">
            <h5 class="card-title">@<%= user.username %></h5>
            <p class="card-text">
              <%= post.content %>
            </p>
            <a
              href="#"
              class="card-link link-primary link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
              >Like</a
            >
            <a
              href="/edit/<%=post._id %>"
              class="card-link link-secondary link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
              >Edit</a
            >
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>
